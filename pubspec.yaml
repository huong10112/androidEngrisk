name: engrisk
description: An English learning application

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.0
  firebase_analytics: ^10.7.0
  cloud_firestore: ^4.8.4
  audioplayers: ^5.2.1
  flutter_svg: ^2.0.9  # Thêm package cho SVG icons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/svgs/whatsapp.svg     # ĐƯỜNG DẪN ĐÚNG
    - assets/svgs/facebook.svg     # ĐƯỜNG DẪN ĐÚNG
    - assets/svgs/google.svg
    - assets/audios/
